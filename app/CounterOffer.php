<?php

namespace App;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class CounterOffer extends Model
{
    protected $gourded = [];
    // use HasFactory;
    // protected $fillable =[
    //     "product_id", "sender_id", "receiver_id", "parent_id", "user_id", "quantity", "used", "expired_date", "is_active"  
    // ];
}
